import { FiltrarPorServiciosPipe } from './filtrar-por-servicios.pipe';

describe('FiltrarPorServiciosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarPorServiciosPipe();
    expect(pipe).toBeTruthy();
  });
});
